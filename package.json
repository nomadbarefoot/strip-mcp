{
  "name": "strip-mcp-playwright-dev",
  "private": true,
  "description": "Local @playwright/mcp for integration tests",
  "devDependencies": {
    "@modelcontextprotocol/server-memory": "^0.6.2",
    "@playwright/mcp": "^0.0.70",
    "wikipedia-mcp": "^1.0.3"
  }
}
